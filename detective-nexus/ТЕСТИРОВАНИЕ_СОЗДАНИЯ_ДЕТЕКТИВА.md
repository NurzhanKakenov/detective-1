# 👮‍♂️ Тестирование создания детектива

## Как протестировать создание нового детектива

### 1. Убедитесь, что у вас есть права
Создание детективов доступно только лейтенантам и выше:
```bash
cd detective-nexus/backend
python manage_roles.py set 1 lieutenant  # или выше
```

### 2. Откройте дашборд
- Перейдите на http://localhost:3000/dashboard
- Обновите страницу (F5) если нужно

### 3. Проверьте права доступа
- Кнопка "Добавить детектива" должна быть активной (зеленая)
- Если кнопка неактивна, проверьте свое звание

### 4. Создайте нового детектива
1. Нажмите кнопку "Добавить детектива"
2. Заполните все обязательные поля:
   - **Имя пользователя**: уникальное имя (например: `DT_III`)
   - **Полное имя**: ФИО детектива (например: `Bob Garfild`)
   - **Email**: email адрес (например: `garfild@gmail.com`)
   - **Звание**: выберите из списка (например: `Старший детектив`)
   - **Номер значка**: уникальный номер (например: `1WM-1`)
   - **Отдел**: выберите отдел
3. Нажмите "Добавить детектива"

### 5. Проверьте результат
После создания:
- ✅ Появится уведомление об успешном создании
- ✅ Модальное окно закроется
- ✅ Форма очистится
- ✅ **Новый детектив появится в админке**

## Что должно произойти

### ✅ Успешное создание
- Кнопка показывает "Добавление..." с анимацией загрузки
- Появляется зеленое уведомление об успехе
- Детектив добавляется в базу данных
- Можно найти нового детектива в админке

### ❌ Если создание не работает

#### 1. Проверьте права доступа
```bash
python manage_roles.py list
# Ваше звание должно быть lieutenant или выше
```

#### 2. Проверьте консоль браузера
- Откройте DevTools (F12) → Console
- Посмотрите на ошибки при нажатии кнопки
- Проверьте Network на запросы к API

#### 3. Проверьте backend
```bash
# Убедитесь что backend запущен
curl http://localhost:8000/health

# Или проверьте логи backend
```

#### 4. Проверьте заполнение формы
- Все поля должны быть заполнены
- Имя пользователя должно быть уникальным
- Email должен быть валидным

## Возможные ошибки

### "User with this Discord ID already exists"
- Система генерирует уникальный discord_id автоматически
- Если ошибка повторяется, попробуйте другое имя пользователя

### "Username already taken"
- Выберите другое имя пользователя
- Имена должны быть уникальными в системе

### "Доступ запрещен"
- Повысьте свое звание до лейтенанта или выше
- Обновите страницу после изменения звания

### Кнопка неактивна
- Заполните все обязательные поля (отмечены звездочкой *)
- Проверьте, что все поля корректно заполнены

## Проверка результата

### В админке:
1. Перейдите на http://localhost:3000/admin
2. Откройте вкладку "Пользователи"
3. Найдите нового детектива в списке
4. Проверьте его данные

### В базе данных:
```bash
cd detective-nexus/backend
python manage_roles.py list
# Новый пользователь должен появиться в списке
```

## Демо-данные для тестирования

Используйте эти данные для быстрого тестирования:

| Поле | Значение |
|------|----------|
| Имя пользователя | `test_detective_new` |
| Полное имя | `Тестовый Новый Детектив` |
| Email | `test.new@detective.com` |
| Звание | `Детектив` |
| Номер значка | `NEW001` |
| Отдел | `Отдел по расследованию убийств` |

## Устранение проблем

### Если ничего не происходит при нажатии кнопки:
1. Проверьте консоль браузера на ошибки JavaScript
2. Убедитесь, что все поля заполнены
3. Проверьте, что backend запущен и отвечает
4. Попробуйте обновить страницу (F5)

### Если появляется ошибка API:
1. Проверьте логи backend в терминале
2. Убедитесь, что порт 8000 свободен
3. Перезапустите backend если нужно

### Если форма не очищается:
- Это нормально при ошибке создания
- При успешном создании форма очищается автоматически
- Можно закрыть и открыть модальное окно заново